import { styled } from "@mui/material";

export const HeaderSlot = styled("header")(() => ({ padding: "20px" }));

export const DivSlot = styled("div")(() => ({
  display: "flex",
  justifyContent: "space-evenly",
}));
